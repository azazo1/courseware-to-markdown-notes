interface:
  display_name: "课件笔记"
  short_description: "逐页读图生成结构清晰的中文课件笔记"
  default_prompt: "使用 $courseware-to-markdown-notes 将这份课件按默认单 agent 路径转换为一份完整, 保留结构且适合初学者阅读的 Markdown 笔记."

policy:
  allow_implicit_invocation: false
