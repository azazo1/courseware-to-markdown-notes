interface:
  display_name: "课件笔记"
  short_description: "将完整课件整理为 Markdown 笔记"
  default_prompt: "使用 $courseware-to-markdown-notes 将这份课件转换为一份完整, 保留结构的 Markdown 笔记, 并用 LaTeX 表示公式."

policy:
  allow_implicit_invocation: false
